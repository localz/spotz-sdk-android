<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.localz.spotz.sdk">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application android:allowBackup="true">
        <receiver android:name=".OnBeaconDiscoveryFoundReceiver" >
            <intent-filter>
                <action android:name="com.localz.spotz.sdk.LOCALZ_BLE_SCAN_FOUND" />
            </intent-filter>
        </receiver>

        <receiver android:name=".OnBeaconDiscoveryFinishedReceiver" >
            <intent-filter>
                <action android:name="com.localz.spotz.sdk.LOCALZ_BLE_SCAN_FINISH" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
